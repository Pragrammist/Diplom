@model WebHost.Views.Shared.Components.LabelComment.LabelCommentModel;
@using static WebHost.Views.ViewsMethodsHelpers;


<div class="cluster-circle cluster-top-element label-circle @GetLabelClass(Model.Label)">
    <a class="text text-right"
        asp-route-action="Details"
        asp-route-productId="@Model.ProductId"
        asp-route-label="@Model.Label"
        asp-route-clusterNumber="@Model.ClusterNumber"
    >
        @Model.Percent
        <span>%</span>
    </a>
</div>